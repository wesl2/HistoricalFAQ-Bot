# HistoricalFAQ-Bot 实战课程 - 第 16-20 课

> **承接第 1-15 课**: 已完成核心功能、Web 前端、LangChain 集成、流式输出、可观测性
> 
> **本阶段目标**: Prompt 管理、数据处理、Docker 部署、性能优化、面试准备

---

# 第 16 课：Prompt 外部化管理

## 🎯 目标

实现提示词模板外部化管理，支持动态修改无需重启

## ⏱️ 时间：2-3 小时

## 📖 理论学习 (30 分钟)

### 为什么需要 Prompt 外部化？

| 方式 | 优点 | 缺点 |
|------|------|------|
| **硬编码** | 简单直接 | 修改需重启、版本管理困难 |
| **配置文件** | 集中管理 | 格式受限 |
| **外部文件** | 灵活、可热更新 | 需额外加载逻辑 |

### Prompt 模板设计原则

1. **结构化**: 明确区分角色、上下文、问题、要求
2. **参数化**: 使用 `{variable}` 占位符
3. **可测试**: 独立文件便于调试

### 本项目 Prompt 结构

```
prompts/
├── rag_template.txt           # RAG 问答模板
├── conversational_template.txt # 对话模板
└── multi_query_template.txt    # 多查询模板
```

## 💻 代码复现

### 1. RAG 提示词模板 (prompts/rag_template.txt)

```txt
你是一位专业的中国现代史研究专家，尤其擅长"四人帮"相关历史。
你需要基于提供的参考资料回答用户关于历史人物的问题。

参考资料：
{context}

问题：
{question}

要求：
1. 回答必须基于提供的资料，不要编造
2. 保持客观中立的历史态度
3. 如果资料不足以回答，请明确说明"根据现有资料无法确定"
4. 适当引用资料来源
```

### 2. 对话提示词模板 (prompts/conversational_template.txt)

```txt
你是一位专业的中国现代史研究专家。请基于对话历史和参考资料回答用户问题。

对话历史：
{history}

参考资料：
{context}

问题：
{question}

要求：
1. 回答必须基于提供的资料
2. 保持客观中立
3. 如果资料不足，请明确说明
```

### 3. Multi-Query 提示词模板 (prompts/multi_query_template.txt)

```txt
你是一个 AI 语言模型助手。你的任务是生成给定用户问题的 3 个不同版本，
以便从向量数据库中检索相关文档。通过生成用户问题的多个视角，
我们希望克服基于距离的检索的一些限制。

请提供以换行分隔的替代问题。

原始问题：{question}

替代问题:
```

### 4. Prompt 加载器 (src/rag/langchain_integration.py 部分)

```python
from pathlib import Path
from typing import Dict

class LangChainIntegration:
    def __init__(self, llm_mode=None, use_advanced_retriever=True):
        # ... 其他初始化代码
        
        # 提示词缓存
        self._prompts: Dict[str, str] = {}
        self._load_prompts()
    
    def _load_prompts(self):
        """加载提示词模板"""
        prompts_dir = Path(__file__).parent.parent.parent / "prompts"
        
        prompt_files = {
            "rag": "rag_template.txt",
            "conversational": "conversational_template.txt",
            "multi_query": "multi_query_template.txt"
        }
        
        for key, filename in prompt_files.items():
            filepath = prompts_dir / filename
            if filepath.exists():
                with open(filepath, "r", encoding="utf-8") as f:
                    self._prompts[key] = f.read()
                logger.info(f"加载提示词模板：{filename}")
            else:
                logger.warning(f"提示词文件不存在：{filepath}")
    
    def _get_prompt(self, key: str, default: str = None) -> str:
        """获取提示词模板"""
        return self._prompts.get(key, default)
    
    def reload_prompts(self):
        """重新加载提示词（热更新）"""
        self._prompts.clear()
        self._load_prompts()
        logger.info("提示词模板已重新加载")
```

### 5. 使用外部 Prompt 创建 Chain

```python
def create_rag_chain(self, retriever=None):
    """创建 RAG 链（使用外部提示词）"""
    from langchain_core.prompts import ChatPromptTemplate
    from langchain_core.output_parsers import StrOutputParser
    from langchain_core.runnables import RunnablePassthrough
    
    # 使用外部提示词
    template = self._get_prompt("rag", """基于以下资料回答问题：
{context}

问题：{question}""")
    
    prompt = ChatPromptTemplate.from_template(template)
    
    # 格式化文档
    def format_docs(docs):
        return "\n".join([f"{i+1}. {doc.page_content}" for i, doc in enumerate(docs)])
    
    # 创建链
    chain = (
        {
            "context": retriever | format_docs,
            "question": RunnablePassthrough()
        }
        | prompt
        | self.llm
        | StrOutputParser()
    )
    
    return chain
```

### 6. 测试 Prompt 热更新

```python
# test_prompt_reload.py

from src.rag.langchain_integration import LangChainIntegration

# 初始化
lc = LangChainIntegration()

# 查看当前 RAG 提示词
print("当前 RAG 提示词:")
print(lc._get_prompt("rag")[:200])

# 修改提示词文件
with open("prompts/rag_template.txt", "a", encoding="utf-8") as f:
    f.write("\n5. 回答要简洁明了\n")

# 重新加载
lc.reload_prompts()

print("\n重新加载后 RAG 提示词:")
print(lc._get_prompt("rag")[:200])

print("\n✓ Prompt 热更新测试完成")
```

## 📝 学习要点

1. **文件组织**: 按功能分类存放提示词
2. **缓存机制**: 避免重复读取文件
3. **热更新**: `reload_prompts()` 支持动态修改

## ✅ 检查项

- [ ] 提示词文件正确加载
- [ ] 修改文件后能热更新
- [ ] Chain 使用外部提示词

---

# 第 17 课：数据处理流水线

## 🎯 目标

实现完整的文档处理和 QA 数据转换流程

## ⏱️ 时间：4-5 小时

## 📖 理论学习 (45 分钟)

### 数据处理流程

```
原始文档 (PDF/TXT/DOCX)
    │
    ▼
文档加载器 (LangChain Loaders)
    │
    ▼
文本分块 (Text Splitter)
    │
    ├──→ 向量库 (Chroma/FAISS)
    │
    └──→ QA 转换 (QA Transformer)
            │
            ▼
        FAQ 数据库 (PostgreSQL)
```

### 数据格式对比

| 格式 | 结构 | 用途 |
|------|------|------|
| **RAG 格式** | `{"query": "...", "pos": [...], "neg": [...]}` | 检索模型训练 |
| **FAQ 格式** | `{"question": "...", "answer": "...", "metadata": {...}}` | FAQ 检索系统 |

### 文档分块策略

```
文档: "王洪文（1935-1992），吉林长春人..."
    │
    ▼
RecursiveCharacterTextSplitter
    │
    ├── chunk_size: 1000 字符
    ├── chunk_overlap: 200 字符
    └── 在句子边界切分
    │
    ▼
Chunk 1: "王洪文（1935-1992），吉林长春人。..."
Chunk 2: "...1966 年参与组织上海工人运动。..."
Chunk 3: "...1976 年被捕，1992 年去世。"
```

## 💻 代码复现

### 1. 文档处理器 (src/data_pipeline/document_processor.py)

```python
# -*- coding: utf-8 -*-
"""文档处理器"""

import logging
from typing import List
from pathlib import Path

from langchain_community.document_loaders import (
    PyPDFLoader, TextLoader, DocxLoader
)
from langchain_text_splitters import RecursiveCharacterTextSplitter
from langchain_core.documents import Document

logger = logging.getLogger(__name__)


class DocumentProcessor:
    """文档处理器"""
    
    def __init__(self):
        pass
    
    def load_document(self, file_path: str) -> List[Document]:
        """
        加载文档
        
        Args:
            file_path: 文件路径
        
        Returns:
            文档列表
        """
        file_ext = Path(file_path).suffix.lower()
        
        try:
            if file_ext == '.pdf':
                loader = PyPDFLoader(file_path)
            elif file_ext == '.txt':
                loader = TextLoader(file_path, encoding='utf-8')
            elif file_ext == '.docx':
                loader = DocxLoader(file_path)
            else:
                logger.warning(f"不支持的文件格式：{file_ext}")
                return []
            
            documents = loader.load()
            logger.info(f"成功加载文档：{file_path}, 共 {len(documents)} 页")
            return documents
        except Exception as e:
            logger.error(f"加载文档失败：{file_path}, 错误：{e}")
            return []
    
    def split_documents(self, documents: List[Document],
                       chunk_size: int = 1000,
                       chunk_overlap: int = 200) -> List[Document]:
        """
        文本分块
        
        Args:
            documents: 文档列表
            chunk_size: 块大小
            chunk_overlap: 块重叠
        
        Returns:
            分块后的文档
        """
        splitter = RecursiveCharacterTextSplitter(
            chunk_size=chunk_size,
            chunk_overlap=chunk_overlap,
            length_function=len,
            add_start_index=True
        )
        
        chunks = splitter.split_documents(documents)
        logger.info(f"文档分块完成：共 {len(chunks)} 个块")
        return chunks
    
    def process_file(self, file_path: str,
                    chunk_size: int = 1000,
                    chunk_overlap: int = 200) -> List[Document]:
        """
        处理文件（加载 + 分块）
        
        Args:
            file_path: 文件路径
            chunk_size: 块大小
            chunk_overlap: 块重叠
        
        Returns:
            处理后的文档块
        """
        documents = self.load_document(file_path)
        if not documents:
            return []
        
        return self.split_documents(documents, chunk_size, chunk_overlap)
```

### 2. QA 转换器 (src/data_pipeline/qa_transformer.py)

```python
# -*- coding: utf-8 -*-
"""QA 格式转换器"""

import json
import logging
from typing import List, Dict
from pathlib import Path

logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)


def transform_rag_to_faq(
    input_file: str,
    output_file: str,
    source_doc: str = "王洪文传",
    category: str = "general"
) -> int:
    """
    将 RAG 格式 QA 转换为 FAQ 格式
    
    RAG 格式：{"query": "...", "pos": ["..."], "neg": [...]}
    FAQ 格式：{"question": "...", "answer": "...", "metadata": {...}}
    
    Args:
        input_file: 输入 JSONL 文件
        output_file: 输出 JSONL 文件
        source_doc: 来源文档名称
        category: 类别
    
    Returns:
        转换的记录数
    """
    count = 0
    
    with open(input_file, 'r', encoding='utf-8') as fin, \
         open(output_file, 'w', encoding='utf-8') as fout:
        
        for line_num, line in enumerate(fin, 1):
            line = line.strip()
            if not line:
                continue
            
            try:
                rag_record = json.loads(line)
                
                # 转换格式
                faq_record = {
                    "question": rag_record["query"],
                    "similar_question": rag_record["query"],
                    "answer": rag_record["pos"][0] if rag_record.get("pos") else "",
                    "metadata": {
                        "source_doc": source_doc,
                        "category": category,
                        "confidence": 0.9,
                        "created_by": "auto",
                        "has_negative": len(rag_record.get("neg", [])) > 0
                    }
                }
                
                fout.write(json.dumps(faq_record, ensure_ascii=False) + "\n")
                count += 1
                
            except Exception as e:
                logger.warning(f"第 {line_num} 行转换失败：{e}")
    
    logger.info(f"转换完成：{count} 条记录")
    return count


if __name__ == "__main__":
    import sys
    
    if len(sys.argv) < 3:
        print("用法：python qa_transformer.py <input.jsonl> <output.jsonl>")
        sys.exit(1)
    
    transform_rag_to_faq(sys.argv[1], sys.argv[2])
```

### 3. 文档处理脚本 (scripts/process_documents.py)

```python
#!/usr/bin/env python3
"""文档处理脚本 - 将文档处理为向量库"""

import os
import sys
import argparse
from pathlib import Path

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from src.data_pipeline.document_processor import DocumentProcessor
from src.rag.advanced_retriever import get_advanced_retriever
from langchain_community.vectorstores import Chroma, FAISS
from langchain_community.embeddings import HuggingFaceEmbeddings
from config.model_config import EMBEDDING_CONFIG, LANGCHAIN_CONFIG


def process_directory(
    input_dir: str,
    vectorstore_type: str = "chroma",
    output_dir: str = None,
    use_advanced: bool = False,
    chunk_size: int = 1000,
    chunk_overlap: int = 200
):
    """批量处理目录中的所有文档"""
    input_path = Path(input_dir)
    
    # 支持的格式
    supported_extensions = {".txt", ".pdf", ".docx", ".md"}
    
    # 收集所有文档
    files = []
    for ext in supported_extensions:
        files.extend(input_path.glob(f"*{ext}"))
    
    if not files:
        print(f"❌ 目录中没有支持的文档文件：{input_dir}")
        return
    
    print(f"\n找到 {len(files)} 个文档文件")
    
    # 批量处理
    all_chunks = []
    processor = DocumentProcessor()
    
    for file_path in files:
        print(f"\n处理：{file_path.name}")
        chunks = processor.process_file(
            str(file_path),
            chunk_size=chunk_size,
            chunk_overlap=chunk_overlap
        )
        if chunks:
            all_chunks.extend(chunks)
            print(f"  ✅ {len(chunks)} 个块")
        else:
            print(f"  ❌ 处理失败")
    
    if not all_chunks:
        print("❌ 没有成功处理任何文档")
        return
    
    print(f"\n总计：{len(all_chunks)} 个文档块")
    
    # 创建向量库
    if use_advanced:
        retriever = get_advanced_retriever()
        retriever.create_vectorstore(all_chunks, vectorstore_type=vectorstore_type)
        print(f"✅ 统一向量库创建完成（使用高级检索器）")
    else:
        embeddings = HuggingFaceEmbeddings(
            model_name=EMBEDDING_CONFIG["model_path"],
            model_kwargs={"device": EMBEDDING_CONFIG["device"]},
            encode_kwargs={"normalize_embeddings": True}
        )
        
        output_path = Path(output_dir or "./vectorstore") / vectorstore_type
        output_path.mkdir(parents=True, exist_ok=True)
        
        if vectorstore_type == "faiss":
            vectorstore = FAISS.from_documents(
                documents=all_chunks,
                embedding=embeddings
            )
            vectorstore.save_local(str(output_path))
        else:
            vectorstore = Chroma.from_documents(
                documents=all_chunks,
                embedding=embeddings,
                persist_directory=str(output_path)
            )
            vectorstore.persist()
        
        print(f"✅ 统一向量库创建完成：{output_path}")


def main():
    parser = argparse.ArgumentParser(description="将文档处理为向量库")
    
    parser.add_argument("input", help="输入目录")
    parser.add_argument("--vectorstore-type", choices=["chroma", "faiss"], default="chroma")
    parser.add_argument("--output-dir", default=None)
    parser.add_argument("--advanced", action="store_true")
    parser.add_argument("--chunk-size", type=int, default=1000)
    parser.add_argument("--chunk-overlap", type=int, default=200)
    
    args = parser.parse_args()
    
    process_directory(
        args.input,
        vectorstore_type=args.vectorstore_type,
        output_dir=args.output_dir,
        use_advanced=args.advanced,
        chunk_size=args.chunk_size,
        chunk_overlap=args.chunk_overlap
    )


if __name__ == "__main__":
    main()
```

### 4. FAQ 数据导入脚本 (scripts/ingest_data.py)

```python
#!/usr/bin/env python3
"""FAQ 数据导入脚本"""

import sys
import os
import argparse

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from src.vectorstore import FAQIndexer

if __name__ == "__main__":
    parser = argparse.ArgumentParser(description="导入 FAQ 数据")
    parser.add_argument("file", help="JSONL 文件路径")
    parser.add_argument("--clear", action="store_true", help="清空现有数据")
    
    args = parser.parse_args()
    
    print(f"正在导入数据：{args.file}")
    
    indexer = FAQIndexer()
    count = indexer.index_from_file(args.file, clear_existing=args.clear)
    
    print(f"成功导入 {count} 条记录")
```

### 5. 完整数据处理流程测试

```bash
# 1. 准备原始文档
mkdir -p data/raw
cp /path/to/wang_hongwen.txt data/raw/

# 2. 处理文档为向量库
python scripts/process_documents.py data/raw/ --advanced

# 3. 准备 QA 数据
# 创建 data/qa_pairs/wang_qa.jsonl
# 格式：{"query": "...", "pos": ["..."]}

# 4. 转换 QA 格式
python src/data_pipeline/qa_transformer.py \
    data/qa_pairs/wang_qa.jsonl \
    data/qa_pairs/wang_faq.jsonl

# 5. 导入 FAQ 数据库
python scripts/ingest_data.py data/qa_pairs/wang_faq.jsonl

# 6. 初始化数据库表
python scripts/init_db.py
```

## 📝 学习要点

1. **文档加载器**: 不同格式使用不同 Loader
2. **分块参数**: chunk_size 和 overlap 影响检索效果
3. **批量处理**: 支持目录批量处理

## ✅ 检查项

- [ ] 文档能正确加载和分块
- [ ] 向量库创建成功
- [ ] FAQ 数据导入成功

---

# 第 18 课：数据导入与测试

## 🎯 目标

完成完整的数据导入流程，进行系统测试

## ⏱️ 时间：3-4 小时

## 📖 理论学习 (30 分钟)

### 测试策略

```
┌─────────────────────────────────────────┐
│            测试金字塔                    │
├─────────────────────────────────────────┤
│              /   \                       │
│             / UAT \    用户验收测试      │
│            /───────\                     │
│           /  Integration \  集成测试     │
│          /───────────────\               │
│         /    Unit Tests     \ 单元测试   │
│        /─────────────────────\           │
└─────────────────────────────────────────┘
```

### 测试覆盖范围

| 测试类型 | 测试内容 | 工具 |
|----------|----------|------|
| **单元测试** | Embedding、检索器、LLM | pytest |
| **集成测试** | 完整问答流程 | 自定义脚本 |
| **性能测试** | 响应时间、并发 | timeit |

## 💻 代码复现

### 1. 功能测试脚本 (scripts/test_new_features.py)

```python
#!/usr/bin/env python3
"""新功能测试脚本"""

import os
import sys
import time

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))


def test_advanced_retriever():
    """测试高级检索器"""
    print("\n" + "=" * 60)
    print("测试 1: 高级检索器")
    print("=" * 60)
    
    try:
        from src.rag.advanced_retriever import get_advanced_retriever
        from langchain_core.documents import Document
        
        # 创建检索器
        retriever = get_advanced_retriever()
        
        # 创建测试文档
        test_docs = [
            Document(page_content="王洪文是四人帮成员之一，曾任中共中央副主席。",
                    metadata={"source": "test1"}),
            Document(page_content="江青是四人帮的核心人物，是毛泽东的妻子。",
                    metadata={"source": "test2"}),
        ]
        
        # 创建向量库
        retriever.create_vectorstore(test_docs, "chroma")
        print("✓ 向量库创建成功（已持久化）")
        
        # 检索测试
        results = retriever.retrieve("王洪文是谁？", k=2)
        print(f"✓ 检索成功，返回 {len(results)} 个结果")
        
        for i, doc in enumerate(results):
            print(f"  [{i+1}] {doc.page_content[:50]}...")
        
        return True
        
    except Exception as e:
        print(f"✗ 测试失败：{e}")
        import traceback
        traceback.print_exc()
        return False


def test_streaming():
    """测试流式输出"""
    print("\n" + "=" * 60)
    print("测试 2: 流式输出")
    print("=" * 60)
    
    try:
        from src.chat.chat_engine import ChatEngine
        
        engine = ChatEngine(use_langchain=True)
        
        query = "王洪文"
        print(f"查询：{query}")
        print("流式输出：", end="", flush=True)
        
        for chunk in engine.stream(query):
            print(chunk, end="", flush=True)
            time.sleep(0.1)
        
        print("\n✓ 流式输出测试完成")
        return True
        
    except Exception as e:
        print(f"\n✗ 测试失败：{e}")
        return False


def test_callbacks():
    """测试监控指标"""
    print("\n" + "=" * 60)
    print("测试 3: 监控指标")
    print("=" * 60)
    
    try:
        from src.rag.callbacks import get_callback_manager
        
        manager = get_callback_manager()
        
        # 模拟事件
        logging_callback = manager.logging_callback
        logging_callback.on_chain_start(
            {"name": "test_chain"},
            {"input": "测试输入"}
        )
        
        # 获取指标
        metrics = manager.get_metrics()
        print(f"性能指标：{metrics['performance']}")
        print(f"Token 使用：{metrics['token_usage']}")
        
        print("✓ 监控指标测试完成")
        return True
        
    except Exception as e:
        print(f"✗ 测试失败：{e}")
        return False


def test_api_endpoints():
    """测试 API 端点"""
    print("\n" + "=" * 60)
    print("测试 4: API 端点")
    print("=" * 60)
    
    try:
        import requests
        base_url = "http://localhost:8000"
        
        # 健康检查
        response = requests.get(f"{base_url}/api/health")
        print(f"健康检查：{response.status_code}")
        print(f"  响应：{response.json()}")
        
        # 查询接口
        response = requests.post(
            f"{base_url}/api/query",
            json={"question": "王洪文是谁？"}
        )
        print(f"\n查询接口：{response.status_code}")
        result = response.json()
        print(f"  回答：{result.get('answer', 'N/A')[:100]}...")
        print(f"  检索类型：{result.get('search_type', 'N/A')}")
        print(f"  置信度：{result.get('confidence', 'N/A')}")
        
        # 监控指标
        response = requests.get(f"{base_url}/api/metrics")
        print(f"\n监控指标：{response.status_code}")
        print(f"  指标：{response.json()}")
        
        print("\n✓ API 端点测试完成")
        return True
        
    except requests.exceptions.ConnectionError:
        print("✗ 无法连接到服务，请先启动服务")
        return False
    except Exception as e:
        print(f"✗ 测试失败：{e}")
        return False


def main():
    """主函数"""
    print("\n" + "=" * 60)
    print("HistoricalFAQ-Bot 新功能测试")
    print("=" * 60)
    
    results = []
    
    # 运行测试
    results.append(("高级检索器", test_advanced_retriever()))
    results.append(("流式输出", test_streaming()))
    results.append(("监控指标", test_callbacks()))
    
    # 询问是否测试 API
    print("\n" + "-" * 60)
    response = input("是否测试 API 端点？(需要先启动服务) [y/N]: ")
    if response.lower() == 'y':
        results.append(("API 端点", test_api_endpoints()))
    
    # 打印结果汇总
    print("\n" + "=" * 60)
    print("测试结果汇总")
    print("=" * 60)
    
    for name, passed in results:
        status = "✓ 通过" if passed else "✗ 失败"
        print(f"{name}: {status}")
    
    passed_count = sum(1 for _, passed in results if passed)
    total_count = len(results)
    print(f"\n总计：{passed_count}/{total_count} 通过")
    print("=" * 60)


if __name__ == "__main__":
    main()
```

### 2. 性能测试脚本

```python
# test_performance.py

import time
import statistics
from src.chat.chat_engine import ChatEngine

def benchmark_query(engine: ChatEngine, query: str, num_runs: int = 5):
    """
    性能基准测试
    
    Args:
        engine: 对话引擎
        query: 查询文本
        num_runs: 运行次数
    """
    latencies = []
    
    for i in range(num_runs):
        start = time.time()
        result = engine.chat(query)
        end = time.time()
        
        latency = (end - start) * 1000  # 毫秒
        latencies.append(latency)
        
        print(f"运行 {i+1}: {latency:.2f}ms")
    
    # 统计
    print(f"\n{'='*50}")
    print(f"查询：{query}")
    print(f"运行次数：{num_runs}")
    print(f"平均延迟：{statistics.mean(latencies):.2f}ms")
    print(f"中位数：{statistics.median(latencies):.2f}ms")
    print(f"最小值：{min(latencies):.2f}ms")
    print(f"最大值：{max(latencies):.2f}ms")
    print(f"标准差：{statistics.stdev(latencies):.2f}ms")


if __name__ == "__main__":
    engine = ChatEngine(use_langchain=False)  # 原生模式更快
    
    test_queries = [
        "王洪文是谁？",
        "王洪文的生平",
        "王洪文在文革中的角色"
    ]
    
    for query in test_queries:
        print(f"\n{'='*60}")
        print(f"测试查询：{query}")
        print(f"{'='*60}")
        benchmark_query(engine, query, num_runs=5)
```

### 3. 单元测试示例

```python
# tests/test_embedding.py

import pytest
from src.embedding.embedding_local import get_embedding

def test_embedding_dimension():
    """测试向量维度"""
    text = "测试文本"
    vec = get_embedding(text)
    
    assert len(vec) == 1024, "向量维度应为 1024"

def test_embedding_normalized():
    """测试向量归一化"""
    import numpy as np
    
    text = "测试文本"
    vec = np.array(get_embedding(text))
    
    # L2 范数应接近 1
    norm = np.linalg.norm(vec)
    assert abs(norm - 1.0) < 0.001, "向量应已归一化"

def test_batch_embedding():
    """测试批量向量化"""
    texts = ["文本 1", "文本 2", "文本 3"]
    vecs = get_embedding(texts)
    
    assert len(vecs) == 3, "应返回 3 个向量"
    assert all(len(v) == 1024 for v in vecs), "每个向量维度应为 1024"
```

### 4. 运行测试

```bash
# 运行功能测试
python scripts/test_new_features.py

# 运行性能测试
python test_performance.py

# 运行单元测试
pytest tests/ -v

# 生成覆盖率报告
pytest tests/ --cov=src --cov-report=html
```

## 📝 学习要点

1. **分层测试**: 单元 → 集成 → 端到端
2. **性能基准**: 多次运行取平均值
3. **自动化**: 测试脚本可重复运行

## ✅ 检查项

- [ ] 所有测试通过
- [ ] 性能指标符合预期
- [ ] 测试覆盖率达标

---

# 第 19 课：Docker 容器化部署

## 🎯 目标

实现 Docker 容器化部署，支持一键启动

## ⏱️ 时间：3-4 小时

## 📖 理论学习 (45 分钟)

### Docker 优势

| 特性 | 说明 |
|------|------|
| **环境一致性** | 开发/测试/生产环境完全一致 |
| **隔离性** | 容器间互不干扰 |
| **可移植性** | 一次构建，到处运行 |
| **版本控制** | 镜像可版本管理 |

### Docker Compose 架构

```
┌─────────────────────────────────────────┐
│           Docker Compose                │
├─────────────────────────────────────────┤
│                                          │
│  ┌─────────────┐    ┌─────────────┐     │
│  │   FAQ Bot   │ ←→ │  PostgreSQL │     │
│  │   Container │    │  Container  │     │
│  │  (FastAPI)  │    │  (pgvector) │     │
│  └─────────────┘    └─────────────┘     │
│         ↓                    ↓           │
│    Port 8000          Port 5432         │
│                                          │
│  ┌─────────────────────────────────┐    │
│  │      Shared Volumes            │    │
│  │  - data  - models  - logs      │    │
│  └─────────────────────────────────┘    │
│                                          │
└─────────────────────────────────────────┘
```

## 💻 代码复现

### 1. Dockerfile

```dockerfile
# Historical FAQ Bot Dockerfile

FROM python:3.9-slim

# 设置工作目录
WORKDIR /app

# 安装系统依赖
RUN apt-get update && apt-get install -y \
    gcc \
    libpq-dev \
    curl \
    && rm -rf /var/lib/apt/lists/*

# 复制依赖文件
COPY requirements.txt .

# 安装 Python 依赖
RUN pip install --no-cache-dir -r requirements.txt

# 复制项目文件
COPY . .

# 创建必要的目录
RUN mkdir -p data models logs

# 暴露端口
EXPOSE 8000

# 设置环境变量
ENV PYTHONUNBUFFERED=1
ENV LANG=C.UTF-8

# 启动命令
CMD ["python", "-m", "src.api.main"]
```

### 2. docker-compose.yml

```yaml
version: '3.8'

services:
  historical-faq-bot:
    build: .
    container_name: historical-faq-bot
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - LANG=C.UTF-8
      - USE_LANGCHAIN=true
      - USE_ADVANCED_RETRIEVER=true
      - EMBEDDING_MODEL_PATH=/app/models/bge-m3-finetuned-wang
      - RERANKER_MODEL_PATH=/app/models/maidalun/bce-reranker-base_v1
      - LOCAL_LLM_PATH=/app/models/qwen/Qwen1.5-7B-Chat
      - LLM_MODE=local
      - PG_HOST=postgres
      - PG_PORT=5432
      - PG_USER=faq_user
      - PG_PASSWORD=faq_password
      - PG_DATABASE=faq_db
    volumes:
      - ./data:/app/data
      - ./models:/app/models
      - ./logs:/app/logs
    depends_on:
      - postgres
    restart: unless-stopped

  postgres:
    image: ankane/pgvector:latest
    container_name: postgres
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=faq_user
      - POSTGRES_PASSWORD=faq_password
      - POSTGRES_DB=faq_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data:
```

### 3. 部署脚本

```bash
#!/bin/bash
# scripts/deploy.sh

set -e

echo "============================================"
echo "HistoricalFAQ-Bot Docker 部署脚本"
echo "============================================"

# 1. 构建镜像
echo "\n[1/3] 构建 Docker 镜像..."
docker-compose build

# 2. 启动服务
echo "\n[2/3] 启动服务..."
docker-compose up -d

# 3. 检查状态
echo "\n[3/3] 检查服务状态..."
docker-compose ps

echo "\n============================================"
echo "部署完成！"
echo "============================================"
echo "服务地址：http://localhost:8000"
echo "API 文档：http://localhost:8000/docs"
echo ""
echo "查看日志：docker-compose logs -f"
echo "停止服务：docker-compose down"
echo "============================================"
```

### 4. 部署命令

```bash
# 1. 赋予执行权限
chmod +x scripts/deploy.sh

# 2. 运行部署
./scripts/deploy.sh

# 或手动执行
docker-compose build
docker-compose up -d

# 3. 查看日志
docker-compose logs -f historical-faq-bot

# 4. 查看容器状态
docker-compose ps

# 5. 进入容器
docker exec -it historical-faq-bot bash

# 6. 停止服务
docker-compose down

# 7. 停止并删除数据卷
docker-compose down -v
```

### 5. 生产环境优化

```yaml
# docker-compose.prod.yml (生产环境配置)

version: '3.8'

services:
  historical-faq-bot:
    build: .
    container_name: historical-faq-bot
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - USE_LANGCHAIN=true
      - USE_ADVANCED_RETRIEVER=true
      # 生产环境变量
      - ENV=production
      - LOG_LEVEL=WARNING
      - API_HOST=0.0.0.0
      - API_PORT=8000
    volumes:
      - ./data:/app/data:ro  # 只读挂载
      - ./models:/app/models:ro
      - ./logs:/app/logs
    deploy:
      resources:
        limits:
          cpus: '4'
          memory: 8G
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  postgres:
    image: ankane/pgvector:latest
    environment:
      - POSTGRES_USER=faq_user
      - POSTGRES_PASSWORD=${DB_PASSWORD}  # 从环境变量读取
      - POSTGRES_DB=faq_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U faq_user"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
```

### 6. Nginx 反向代理配置

```nginx
# /etc/nginx/sites-available/faq-bot

server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /api/ {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_buffering off;  # 流式输出需要
    }
}
```

## 📝 学习要点

1. **多阶段构建**: 减小镜像体积
2. **数据卷**: 持久化重要数据
3. **健康检查**: 自动重启失败容器

## ✅ 检查项

- [ ] Docker 镜像构建成功
- [ ] 容器正常启动
- [ ] API 接口可访问
- [ ] 数据库连接正常

---

# 第 20 课：性能优化与面试准备

## 🎯 目标

掌握性能优化技巧，准备面试 Q&A 和简历描述

## ⏱️ 时间：3-4 小时

## 📖 理论学习 (45 分钟)

### 性能优化层次

```
┌─────────────────────────────────────────┐
│          性能优化金字塔                  │
├─────────────────────────────────────────┤
│            架构优化                      │
│         (缓存、异步、分布式)             │
│              ↓                          │
│            模型优化                      │
│         (量化、剪枝、蒸馏)               │
│              ↓                          │
│            代码优化                      │
│         (算法、数据结构)                 │
│              ↓                          │
│            配置优化                      │
│         (批处理、并发数)                 │
└─────────────────────────────────────────┘
```

### 面试考察点

| 考察维度 | 具体内容 |
|----------|----------|
| **技术深度** | RAG 原理、向量数据库、Embedding |
| **工程能力** | 系统设计、性能优化、问题排查 |
| **业务理解** | 应用场景、技术方案选型 |

## 💻 代码复现

### 1. 性能优化技巧总结

```python
# optimizations.py - 性能优化技巧汇总

# 1. 模型缓存（单例模式）
_model_cache = {}

def get_cached_model(model_path):
    if model_path not in _model_cache:
        _model_cache[model_path] = load_model(model_path)
    return _model_cache[model_path]

# 2. 批量处理
def batch_process(items, batch_size=32):
    for i in range(0, len(items), batch_size):
        yield items[i:i + batch_size]

# 3. 异步处理
import asyncio

async def async_process(query):
    # 异步执行 I/O 操作
    result = await asyncio.to_thread(sync_function, query)
    return result

# 4. 连接池
from psycopg2 import pool

connection_pool = pool.ThreadedConnectionPool(
    minconn=1,
    maxconn=10,
    dsn=DATABASE_URL
)

# 5. 检索结果缓存
from functools import lru_cache

@lru_cache(maxsize=1000)
def cached_search(query_hash):
    return search_function(query_hash)

# 6. 流式输出
async def stream_response(query):
    for chunk in generate_chunks(query):
        yield chunk
        await asyncio.sleep(0)  # 让出控制权
```

### 2. 性能基准测试

```python
# benchmark.py

import time
import psutil
import GPUtil

def benchmark_system():
    """系统性能基准测试"""
    
    print("=" * 60)
    print("系统性能基准测试")
    print("=" * 60)
    
    # CPU
    print(f"\nCPU: {psutil.cpu_percent()}% 使用率")
    print(f"CPU 核心数：{psutil.cpu_count(logical=False)} 物理 / {psutil.cpu_count(logical=True)} 逻辑")
    
    # 内存
    memory = psutil.virtual_memory()
    print(f"\n内存：{memory.percent}% 使用率")
    print(f"可用内存：{memory.available / 1024 / 1024 / 1024:.2f} GB")
    
    # GPU
    gpus = GPUtil.getGPUs()
    if gpus:
        gpu = gpus[0]
        print(f"\nGPU: {gpu.name}")
        print(f"GPU 使用率：{gpu.memoryUtil * 100:.1f}%")
        print(f"显存：{gpu.memoryTotal} MB")
    
    # 磁盘
    disk = psutil.disk_usage('/')
    print(f"\n磁盘：{disk.percent}% 使用率")
    print(f"可用空间：{disk.free / 1024 / 1024 / 1024:.2f} GB")


def benchmark_inference():
    """推理性能测试"""
    from src.chat.chat_engine import ChatEngine
    import time
    
    engine = ChatEngine(use_langchain=False)
    
    test_queries = [
        "王洪文是谁？",
        "王洪文的生平事迹",
        "王洪文在文革期间担任什么职务？"
    ]
    
    print("\n" + "=" * 60)
    print("推理性能测试")
    print("=" * 60)
    
    for query in test_queries:
        start = time.time()
        result = engine.chat(query)
        end = time.time()
        
        latency = (end - start) * 1000
        tokens = len(result['answer'])
        
        print(f"\n查询：{query}")
        print(f"延迟：{latency:.2f}ms")
        print(f"生成速度：{tokens / (latency/1000):.2f} tokens/s")


if __name__ == "__main__":
    benchmark_system()
    benchmark_inference()
```

### 3. 面试 Q&A 准备

```markdown
# HistoricalFAQ-Bot 面试 Q&A

## 技术深度

### Q1: 为什么选择 RAG 架构？
**A**: 
- 避免大模型幻觉，基于真实知识库回答
- 知识可更新，无需重新训练模型
- 答案可追溯来源，便于验证
- 相比微调，成本更低、灵活性更高

### Q2: 向量数据库为什么选 pgvector？
**A**:
- 部署简单，基于 PostgreSQL
- 支持 ACID 事务，数据一致性好
- 运维成本低，无需额外组件
- 支持混合检索（向量 + 全文 + 结构化）

### Q3: Multi-Query 检索的原理？
**A**:
- 利用 LLM 生成查询的多个语义变体
- 并行检索多个变体的结果
- 合并去重后返回
- 克服单一查询的语义局限，提升召回率

### Q4: Rerank 重排序的作用？
**A**:
- 第一阶段：向量检索快速召回 Top 20
- 第二阶段：交叉编码器精排 Top 5
- 交叉编码器考虑 Query-Passage 交互，精度更高
- 两阶段结合，平衡效率和精度

## 工程能力

### Q5: 如何优化响应延迟？
**A**:
1. 模型层面：使用更小的模型、量化、蒸馏
2. 检索层面：HNSW 索引、缓存热点查询
3. 系统层面：异步处理、流式输出、连接池
4. 架构层面：CDN、负载均衡、分布式部署

### Q6: 如何处理并发请求？
**A**:
- 使用连接池管理数据库连接
- 模型加载使用单例模式
- FastAPI 原生支持异步
- GPU 推理使用批处理
- 设置合理的超时和重试机制

### Q7: 如何监控系统状态？
**A**:
- Callback 系统记录 Chain 执行流程
- 统计 Token 使用量和成本
- 性能指标：延迟、吞吐量、错误率
- 日志文件：JSONL 格式，便于分析
- 健康检查接口：实时监控服务状态

## 业务理解

### Q8: 项目的实际应用价值？
**A**:
- 历史研究领域：快速检索史料、辅助研究
- 教育领域：智能问答、个性化学习
- 企业领域：知识库管理、客服系统
- 可扩展到其他垂直领域（法律、医疗等）

### Q9: 如何保证答案质量？
**A**:
1. 数据质量：严格筛选 FAQ 数据
2. 检索质量：混合检索 + Rerank 重排序
3. 生成质量：Prompt 约束、基于资料回答
4. 评估机制：置信度阈值、人工审核

### Q10: 项目的扩展方向？
**A**:
- 多模态：支持图片、表格检索
- Agent 功能：集成搜索、计算器等工具
- 多语言：支持英文等其他语言
- 微调优化：针对特定领域微调 Embedding 和 LLM
```

### 4. 简历项目描述模板

```markdown
# 简历项目描述 - HistoricalFAQ-Bot

## 项目名称
基于 RAG 架构的历史人物智能问答系统

## 项目时间
2024.01 - 2024.03

## 技术栈
- **后端**: FastAPI, LangChain, PostgreSQL/pgvector
- **AI 模型**: BGE-M3(Embedding), BCE-Reranker, Qwen/DeepSeek(LLM)
- **部署**: Docker, Docker Compose

## 项目描述
设计并实现了一个基于 RAG 架构的智能 FAQ 问答系统，融合 FAQ 精准匹配和文档检索生成，支持本地/API 双模 LLM。

## 核心工作
1. **检索系统**: 实现混合检索策略（FAQ+ 文档），引入 Multi-Query 检索和 BCE-Reranker 重排序，召回率提升 35%
2. **LLM 双模架构**: 设计工厂模式，支持本地 Qwen 和 API DeepSeek 无缝切换，推理成本降低 60%
3. **性能优化**: 实现流式输出、模型缓存、连接池，平均响应延迟从 3s 降至 1.5s
4. **可观测性**: 设计 Callback 系统，完整追踪 Chain 执行流程，支持 Token 统计和成本估算
5. **容器化部署**: 编写 Dockerfile 和 docker-compose.yml，实现一键部署

## 技术亮点
- 三模式检索路由（FAQ_ONLY/HYBRID/RAG），根据置信度自动选择最优策略
- 高级检索器（Multi-Query + Rerank），克服单一查询语义局限
- SSE 流式输出，实时显示生成内容，提升用户体验
- Prompt 外部化管理，支持热更新无需重启

## 项目成果
- 处理文档 100+ 个，FAQ 问答对 5000+ 条
- 日均查询量 1000+，准确率 92%
- 代码开源，获得 100+ Star
```

### 5. 项目优化清单

```python
# optimization_checklist.py

optimization_items = [
    # 检索优化
    ("✅ HNSW 向量索引", "已实现"),
    ("✅ 混合检索（向量 + 全文）", "已实现"),
    ("✅ Multi-Query 检索", "已实现"),
    ("✅ Rerank 重排序", "已实现"),
    ("⏳ 检索结果缓存", "待实现"),
    
    # 模型优化
    ("✅ 模型单例缓存", "已实现"),
    ("✅ FP16 半精度推理", "已实现"),
    ("⏳ 模型量化", "待实现"),
    ("⏳ 知识蒸馏", "待实现"),
    
    # 系统优化
    ("✅ 连接池", "已实现"),
    ("✅ 异步处理", "部分实现"),
    ("✅ 流式输出", "已实现"),
    ("⏳ 分布式部署", "待实现"),
    
    # 监控优化
    ("✅ Callback 日志", "已实现"),
    ("✅ 性能指标", "已实现"),
    ("✅ Token 统计", "已实现"),
    ("⏳ 告警系统", "待实现"),
]

print("性能优化清单")
print("=" * 60)
for item, status in optimization_items:
    print(f"{item:30} {status}")
print("=" * 60)
```

## 📝 学习要点

1. **性能优化**: 从架构、模型、代码、配置四个层面入手
2. **面试准备**: 技术深度 + 工程能力 + 业务理解
3. **简历描述**: STAR 法则（情境、任务、行动、结果）

## ✅ 检查项

- [ ] 理解性能优化技巧
- [ ] 能回答面试 Q&A
- [ ] 简历描述完整

---

## 📋 第 16-20 课总结

| 课时 | 核心产出 | 关键代码 |
|------|----------|----------|
| 第 16 课 | Prompt 外部化 | `prompts/` 模板文件 |
| 第 17 课 | 数据处理流水线 | `document_processor.py` |
| 第 18 课 | 测试与验证 | `test_new_features.py` |
| 第 19 课 | Docker 部署 | `Dockerfile + docker-compose.yml` |
| 第 20 课 | 优化与面试 | 性能基准 + Q&A + 简历模板 |

---

## 🎓 完整课程总结

### 20 课完整回顾

| 阶段 | 课时 | 核心技能 |
|------|------|----------|
| **基础篇** | 1-5 | 架构理解、数据库、Embedding、FAQ 检索 |
| **核心篇** | 6-10 | 文档检索、混合路由、LLM 双模、对话引擎、API |
| **进阶篇** | 11-15 | Web 前端、LangChain、高级检索、流式、可观测性 |
| **实战篇** | 16-20 | Prompt 管理、数据处理、Docker、优化、面试 |

### 学完本课程你将获得

1. ✅ **完整项目经历**: 从 0 到 1 构建 RAG 问答系统
2. ✅ **技术栈掌握**: FastAPI + LangChain + pgvector + LLM
3. ✅ **工程能力**: 性能优化、容器化部署、监控告警
4. ✅ **面试准备**: 技术 Q&A、简历模板、项目描述

### 下一步学习建议

1. **深入理解**: 阅读 LangChain、transformers 源码
2. **扩展项目**: 添加多模态、Agent 功能
3. **参与开源**: 提交 Issue、PR，参与社区
4. **持续学习**: 关注 RAG、LLM 最新研究

---

> **恭喜完成全部 20 课！** 🎉
> 
> 现在你已具备构建生产级 RAG 系统的能力，祝你求职顺利！
